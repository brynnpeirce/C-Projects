@model IEnumerable<CarInsurance2.Models.Insuree>

@{ 
    ViewBag.Title = "Admin";
}


@foreach (var item in Model)
{
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LastName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Quote)
        </td>
    </tr>
}
